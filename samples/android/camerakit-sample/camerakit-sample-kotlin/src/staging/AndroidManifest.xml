<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <meta-data android:name="com.snap.camerakit.remote.service.gateway" android:value="${cameraKitRemoteServiceGateway}" />
        <meta-data android:name="com.snap.camerakit.remote.service.authority" android:value="${cameraKitRemoteServiceAuthority}" />
        <meta-data android:name="com.snap.camerakit.remote.access.token" android:value="${cameraKitRemoteAccessToken}" />
    </application>

</manifest>
